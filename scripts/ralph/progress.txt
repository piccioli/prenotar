# Ralph Progress Log
Started: Dom 12 Lug 2026 14:47:50 CEST
---

## Codebase Patterns
- `./vendor/bin/sail ps --status running --services` restituisce l'elenco dei servizi Sail attivi (vuoto se lo stack è down) — utile per script che devono verificare se Sail è in esecuzione prima di lanciare comandi.
- Pint config in `pint.json` è preset "laravel" + `declare_strict_types` + `no_unused_imports` + `ordered_imports` — qualsiasi file PHP nuovo deve rispettare `declare(strict_types=1)` in testa.

## [2026-07-12] - US-001
- Implementato hook di pre-commit locale che lancia `./vendor/bin/sail composer qa` (Pint + Larastan + Pest) e blocca il commit se fallisce.
- Files changed: `.githooks/pre-commit` (nuovo, eseguibile), `CLAUDE.md` (nuova sezione "Hook di pre-commit (QA obbligatoria)" con istruzione di attivazione `git config core.hooksPath .githooks`).
- Verifica manuale eseguita: `git config core.hooksPath .githooks` + commit di prova con un file PHP che violava Pint → commit bloccato (nessun nuovo commit in `git log`, hook ha stampato l'errore Pint e restituito exit non-zero). File di scratch rimosso subito dopo la verifica, non incluso nel commit reale.
- **Learnings for future iterations:**
  - Lo script pre-commit controlla lo stato Sail con `sail ps --status running --services`: se l'output è vuoto (nessun servizio attivo) stampa un messaggio chiaro invece di lasciare fallire `sail composer qa` con un errore Docker criptico.
  - Larastan (`sail composer phpstan`) resta a 0 errori su 156 file dopo la modifica: nessun nuovo codice PHP applicativo introdotto da questa story, solo uno script bash e doc.
---

## [2026-07-12] - US-002
- Rinominato l'intero stack "staging" in "develop": `git mv docker-compose.staging.yml docker-compose.develop.yml`, `git mv .env.staging.example .env.develop.example`, con sostituzione di tutte le occorrenze `staging`→`develop` nei due file (nomi volumi, `env_file`, commenti, `APP_ENV`, `DB_DATABASE`/`DB_USERNAME`=`prenotar_develop`, `CACHE_PREFIX=prenotar_develop_`, `APP_URL`/`MAIL_FROM_ADDRESS`=`develop.prenotar.montagnaservizi.it`).
- Aggiornati tutti i riferimenti attivi al vecchio nome: `DEPLOY.md` (sezione "Ambiente staging (UAT)" → "Ambiente develop", tutti i comandi e l'esempio Nginx), `CLAUDE.md` (roadmap Fase 8), `database/seeders/LocalDevSeeder.php` (whitelist ambienti `['local','testing','staging']` → `['local','testing','develop']`), `UAT_CHECKLIST.md`, `docs/COLLAUDO_wizard-prenotazione-miglioramenti.md`, `docs/ACCOUNT_TEST_LOCALE.md` (dominio da non usare in locale + link a DEPLOY.md).
- Aggiunta voce in `CHANGELOG.md` sotto `## [Non rilasciato]` che documenta il rename.
- Files changed: `docker-compose.develop.yml` (rinominato da `docker-compose.staging.yml`), `.env.develop.example` (rinominato da `.env.staging.example`), `DEPLOY.md`, `CLAUDE.md`, `CHANGELOG.md`, `UAT_CHECKLIST.md`, `docs/COLLAUDO_wizard-prenotazione-miglioramenti.md`, `docs/ACCOUNT_TEST_LOCALE.md`, `database/seeders/LocalDevSeeder.php`.
- Verifica: `grep -rln 'staging'` su tutto il repo (esclusi `.git/`, `node_modules/`, `vendor/`) produce solo hit in `CHANGELOG.md` (voci storiche di release passate + la nuova voce che documenta il rename, entrambe intenzionalmente non riscritte), `tasks/*.md` (PRD storici, esplicitamente esentati dal criterio di accettazione) e `scripts/ralph/prd.json`/`scripts/ralph/archive/.../prd.json` (la spec stessa di questa story, materiale di pianificazione non stack). Nessun residuo su file di codice/config attivi.
- `sail composer qa` verde: Pint OK, Larastan 0 errori su 156 file, Pest 235 test passati (495 assertion).
- **Learnings for future iterations:**
  - Quando si rinomina un ambiente/stack, cercare "staging" non solo in docker-compose/.env ma anche in doc di collaudo/UAT (`UAT_CHECKLIST.md`, `docs/COLLAUDO_*.md`) e nella whitelist ambienti dei seeder (`app()->environment([...])`) — sono facili da dimenticare e non vengono presi da un grep mirato ai soli file di config.
  - Nel CHANGELOG.md le voci storiche (release già taggate) NON vanno riscritte quando si rinomina qualcosa: si aggiunge solo una nuova voce in "## [Non rilasciato]" che documenta il rename. Stesso principio già applicato ai PRD in `tasks/` (riferimento storico, esplicitamente esentato dal grep di verifica).
---

## [2026-07-12] - US-003
- Creato branch locale `develop` a partire da `main` (`git branch develop main`, non pushato — non richiesto esplicitamente dall'utente).
- Verificato `.github/workflows/ci.yml`: il trigger `pull_request:` non ha filtro `branches`, quindi copre già automaticamente le PR verso `develop` e verso `main` senza alcuna modifica al file.
- Aggiunta nuova sezione "Branching e workflow" in `CLAUDE.md` (tra "Hook di pre-commit" e "Convenzioni codice"): documenta che tutto lo sviluppo parte da `develop`, che `main` riceve PR solo da `develop` o da `hotfix/*`, e richiama il comportamento del trigger CI.
- Files changed: `CLAUDE.md` (nuova sezione), branch git `develop` (creato, nessun file modificato da questo).
- `sail composer qa` verde: Pint OK, Larastan 0 errori su 156 file, Pest 235 test passati (495 assertion) — nessun test nuovo necessario, story è solo branching/documentazione.
- **Learnings for future iterations:**
  - Il workflow CI esistente (`ci.yml`) già copre PR verso qualsiasi branch target grazie all'assenza di `branches:` sotto `pull_request:` — le prossime story su develop/CD (US-004..US-010) non devono toccare questo trigger, solo aggiungere nuovi workflow CD dedicati.
  - Quando una story chiede di documentare workflow/branching in CLAUDE.md, la sezione va posizionata vicino alle altre sezioni operative (pre-commit hook, convenzioni codice) per restare coerente con la struttura esistente del file.
---
