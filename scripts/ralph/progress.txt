# Ralph Progress Log
Started: 2026-07-12
---

## Codebase Patterns
- `Filament\Support\Colors\Color::hex('#rrggbb')` generates a full 50–950 shade
  palette from a single brand hex via `Color::generateShades()`: shade 500 is
  exactly the input color, every other shade (including 600, the default Filament
  "primary" reference shade) is algorithmically lightened/darkened from it — don't
  expect the exact input hex to reappear verbatim at 600 in the rendered CSS
  variables, that is expected Filament behaviour, not a bug.
- `->viteTheme('resources/css/filament/{panel}/theme.css')` resolves through
  Laravel's own `Illuminate\Foundation\Vite` invokable against the **default**
  `public/build/manifest.json` unless a custom `buildDirectory` (2nd arg) is
  passed — and passing one requires actually configuring `laravel-vite-plugin`
  to emit a separate manifest under that subdirectory. With a single shared
  `vite.config.js` input array (all 3 panel `theme.css` entries in one build),
  omit the 2nd arg entirely; all 3 panels resolve fine against the one manifest
  Vite already produces.
- Tailwind's `@config 'path.js';` directive must come **after** all `@import`
  statements in the same CSS file (postcss-import enforces "@import must precede
  all other statements"), otherwise `vite build` throws a warning per malformed
  file. Order per theme entry: `@import` (Filament base + our tokens) first,
  then `@config`, then any panel-specific `:root {}` overrides.
- Font self-hosting (no external CDN calls at runtime): use an npm package like
  `@fontsource/<font>` and `@import '@fontsource/<font>/latin.css';` from a CSS
  file processed by Vite — Vite's CSS pipeline resolves the node_modules import,
  rewrites the internal relative `url()`s and copies the woff/woff2 files into
  `public/build/assets/` with hashed names automatically. No manual asset
  copying needed. The per-subset files (`latin.css`, not `400.css` which bundles
  every unicode range including cyrillic/greek/vietnamese) keep the bundle lean
  when only Italian/Latin content is needed.
- `./vendor/bin/sail npm install` / `sail npm run build` work fine even though
  `node_modules` doesn't exist yet on host — Sail's `laravel.test` container has
  its own Node/npm, no need to install anything on the host machine.
- Design token source-of-truth files live in `tasks/design-reference/*/tokens/*.css`
  but per that folder's own README they are "materiale di progettazione ...
  non renderizzabile standalone ... non va servito dall'app": don't `@import`
  them directly into a compiled bundle. Copy the values 1:1 into the app's own
  `resources/css/...` tree (with a comment noting the source of truth), so the
  design-reference folder is read-only reference material, never shipped.
- Extending Filament panel layout without forking the package: use
  `Panel::renderHook(\Filament\View\PanelsRenderHook::X, fn (): string => view(...)->render())`
  in the `*PanelProvider::panel()` chain (list of slots in
  `vendor/filament/filament/src/View/PanelsRenderHook.php`, e.g. `TOPBAR_START`,
  `TOPBAR_END`, `BODY_END`). These closures run on every request (not cached),
  so route-based "is this nav item active" logic can live inline via
  `request()->routeIs('filament.<panel>.pages.dashboard')` /
  `request()->routeIs([...])` (array = OR match) with no extra state needed.
  `renderHook()` also accepts a `$scopes` 3rd arg (page/Livewire component
  class name(s)) to target a hook at one specific page only — useful for
  page-specific additions (e.g. a FAB) without touching the shared panel-wide
  hooks.
- With Filament's default **sidebar** navigation (not top navigation), the
  brand logo/name is rendered inside the sidebar header, NOT in the topbar —
  on mobile, with the sidebar collapsed off-canvas by default, the topbar
  shows only the hamburger button and nothing else unless you inject
  something via `TOPBAR_START`/`TOPBAR_END`.
- Resource route names follow `filament.<panel>.resources.<slug>.<index|create|view|edit>`
  where `<slug>` is the auto-pluralized model name (e.g. `Prenotazione` →
  `prenotaziones`, `Torre` → `torres`, not Italian-correct plurals) and page
  routes follow `filament.<panel>.pages.<page-slug>` (dashboard included, e.g.
  `filament.sezione.pages.dashboard`) — confirmed via
  `sail artisan route:list`, don't guess these from the resource/page class name.
- No MCP browser/screenshot tool available in this environment (confirmed
  again this session): for "visual verification" ACs, use
  `sail artisan tinker --execute="..."` to build a `Request`, call
  `app()->handle($request)` as an authenticated seeded user, and grep the
  returned HTML for the expected markup/classes — this validates the render
  hooks and Blade actually execute end-to-end (not just that Pest's
  `RefreshDatabase`-backed feature tests happen to hit the same code path),
  without a real screenshot. Still explicitly note in progress.txt that pixel
  comparison against the mockup is deferred to manual review.
- Per-row dynamic (non-palette) colors on a Filament `TextColumn`/`TextEntry`
  badge: `->color()` accepts a closure returning a `Filament\Support\Colors\Color::hex($hex)`
  array (the 50–950 shade map), evaluated per-row via Filament's own closure
  injection (typed `$record` param resolved automatically, no manual binding
  needed) — this renders via inline CSS custom properties
  (`vendor/filament/support/src/helpers.php` `get_color_css_variables()`), NOT
  via the registered-palette-name system, so no `FilamentColor::register()` or
  Tailwind safelisting is needed for arbitrary hex values coming from a DB
  column. A **raw hex string** passed directly to `->color()` does NOT work
  (it's treated as a palette *name* lookup, e.g. `var(--#1D574B-500)`, which
  doesn't exist) — always wrap with `Color::hex()` first.
- Larastan gotcha: `$record->torre?->colore_hex ?? 'fallback'` (nullsafe on a
  `BelongsTo` magic relation property, combined with `??`) trips the
  `nullsafe.neverNull` rule even when the FK column genuinely is nullable in
  the migration (`foreignId('torre_id')->nullable()`) — Larastan's relation
  type inference for magic properties doesn't factor in DB-level nullability
  here, so it wrongly claims the relation is never null. Don't blindly follow
  its "use -> instead" suggestion (that produces a real null-pointer risk at
  runtime, since the relation genuinely can be null). Instead extract a small
  static helper with an explicitly nullable typed parameter, e.g.
  `Model::helperMethod(?self $model): string { return $model === null ? ... : ...; }`,
  and call it as `Helper::helperMethod($record->relation)` (plain `->`, no
  nullsafe operator at the call site) — this satisfies Larastan AND centralizes
  the "what's the fallback for a missing related record" logic in one place
  instead of duplicating a ternary at every call site.

## [2026-07-12] - US-001
- Nuovo tema Filament compilato via Vite per i 3 pannelli, con i design token del progetto (`tasks/design-reference/prenotar-ui-ruoli/tokens/*.css`) copiati 1:1 in `resources/css/filament/tokens/{colors,typography,spacing,effects,base}.css` + `fonts.css` (Manrope self-hosted via `@fontsource/manrope`, subset `latin` per weight 200-800) + `index.css` che li aggrega.
- Un `theme.css` per pannello (`resources/css/filament/{admin,gr,sezione}/theme.css`): importano il tema base di Filament + i token condivisi, referenziano un `tailwind.config.js` condiviso (`resources/css/filament/tailwind.config.js`, preset Filament + content sui 3 pannelli), e sovrascrivono solo gli alias semantici brand (`--text-brand`, `--surface-brand`, `--surface-brand-strong`, `--surface-inverse`, `--border-strong`, `--focus-ring`, `--surface-subtle`) col colore del pannello: GR usa il default pino (`--green-*`, già la scala condivisa), Admin sovrascrive con la scala larice esistente (`--larch-*`), Sezione introduce una nuova scala "ardesia" (`#1A3448`…`#F0F6FA`, campionata dal mockup `pannello-sezione.dc.html`, non presente nel file token condiviso perché specifica di questo pannello).
- I 3 `*PanelProvider.php` (`AdminPanelProvider`, `GrPanelProvider`, `SezionePanelProvider`) sostituiscono `Color::Amber/Green/Blue` (palette stock Filament) con `Color::hex($hexDelPannello)` (Admin `#C77E2A` larice, GR `#1D574B` pino, Sezione `#2E5878` ardesia — hex letti dagli swatch nei rispettivi mockup) e aggiungono `->viteTheme('resources/css/filament/{panel}/theme.css')`.
- `vite.config.js`: aggiunti i 3 nuovi entry point CSS all'array `input` del plugin laravel-vite-plugin (build unico, un solo manifest condiviso).
- Nuove devDependency npm: `@fontsource/manrope`, `@tailwindcss/forms`, `@tailwindcss/typography` (richieste dal preset Tailwind di `filament/support`, non ancora installate nel repo). `package-lock.json` generato per la prima volta (`node_modules` non era mai stato installato in questo repo).
- Files changed: `app/Providers/Filament/{Admin,Gr,Sezione}PanelProvider.php`, `vite.config.js`, `package.json`, `package-lock.json` (nuovo), `resources/css/filament/**` (nuovo: `tokens/{index,fonts,colors,typography,spacing,effects,base}.css`, `tailwind.config.js`, `{admin,gr,sezione}/theme.css`).
- Verifica: `sail npm run build` produce un manifest con i 3 `theme-*.css` distinti + i file font `manrope-latin-*.woff2/woff` con nomi hashati; `curl` sulle 3 login page (`/admin/login`, `/gr/login`, `/sezione/login`) conferma che ciascuna carica un asset `theme-*.css` diverso e che il CSS runtime di Filament genera una palette `--primary-*` coerente col hex custom di quel pannello. Nessun tool MCP di screenshot/browser disponibile in questa sessione: la verifica visiva a schermo (confronto pixel con i mockup) resta da fare manualmente in un secondo momento.
- `sail composer qa` verde: Pint OK, Larastan 0 errori su 156 file, Pest 235 test passati (495 assertion) — nessuna regressione, nessun test esistente dipendeva dai colori stock Filament.
- **Learnings for future iterations:**
  - Le prossime story (US-002+) che restylano markup Blade dovranno referenziare le stesse CSS custom properties introdotte qui (`--text-brand`, `--surface-*`, `--border-strong`, token di spacing/effects) invece di hardcodare nuovi colori: sono già coerenti per pannello grazie all'override in ciascun `theme.css`.
  - La scala "ardesia" del pannello Sezione non esiste nel file token condiviso (`tasks/design-reference/.../tokens/colors.css` copre solo pino/larice/stone): è stata derivata a mano dai valori hex effettivamente usati nel mockup `pannello-sezione.dc.html` (grep dei valori `#RRGGBB` nel file) e vive solo in `resources/css/filament/sezione/theme.css`, non nel `tokens/colors.css` condiviso.
  - Se una story futura ha bisogno di una verifica visiva reale (screenshot/confronto mockup) e non c'è un tool MCP browser disponibile, annotarlo esplicitamente qui invece di affermare che la verifica visiva è stata completata — vedi nota sopra.
---

## [2026-07-12] - US-002
- Shell mobile condivisa per i 3 pannelli (Admin/GR/Sezione), implementata via `Panel::renderHook()` (nessun fork di Filament, nessun vendor:publish): topbar mobile con logo/nome pannello + icona notifiche, bottom tab bar fissa a 4 voci, tutto attivo solo sotto 768px (`md:hidden`).
- 3 nuovi Blade partial condivisi in `resources/views/filament/components/`: `mobile-topbar-brand.blade.php` (logo + `filament()->getBrandName()`, agganciato su `TOPBAR_START`), `mobile-topbar-notifications.blade.php` (icona campanella statica/disabled, non esiste ancora un canale di notifiche reale — fuori scope di questo restyle puramente visivo — agganciato su `TOPBAR_END`), `mobile-bottom-nav.blade.php` (nav a 4 voci, riceve `$items` già risolti dal PanelProvider, agganciato su `BODY_END`).
- I 3 `*PanelProvider.php` aggiungono i 3 `->renderHook(...)` sopra + un metodo privato statico `mobileNavItems()` che costruisce i 4 item (label/icon/url/active) per quel pannello, usando `Resource::getUrl()` / `Page::getUrl()` esistenti (nessun URL hardcoded) e `request()->routeIs(...)` per lo stato attivo: Sezione = Home/Nuova/Prenotazioni/Calendario, GR = Home/Prenotazioni/Calendario/Impostazioni, Admin = Home/Utenti/Prenotazioni/Audit log (icone Heroicons outline: home, plus, clipboard-document-list, calendar-days, cog-6-tooth, users, document-text).
- Nuovo `resources/css/filament/mobile-shell.css` (importato da tutti e 3 i `theme.css`, dopo i token condivisi e prima di `@config`): aggiunge solo `padding-bottom` a `.fi-main` sotto 767px perché il contenuto non finisca sotto la bottom nav fissa.
- Tap target: bottom nav items con `min-h-[44px]` (colonne di una grid a 4, larghezza già ampiamente >44px); l'hamburger esistente è quello nativo di Filament, non toccato.
- FAB: **non implementato in questa story** — nel mockup compare solo nella schermata "Le mie prenotazioni" mobile a card (US-013, non ancora esistente), non nella dashboard/wizard già presenti. La shell supporta il pattern per aggiungerlo in futuro tramite `renderHook(PanelsRenderHook::BODY_END, ..., scopes: [ListPrenotazioni::class])` scoped alla sola pagina che ne ha bisogno (vedi Codebase Patterns sopra), senza toccare `mobile-bottom-nav.blade.php`. Deciso così per non introdurre un meccanismo generico non richiesto da nessuna pagina esistente.
- Files changed: `app/Providers/Filament/{Admin,Gr,Sezione}PanelProvider.php`, `resources/views/filament/components/mobile-{topbar-brand,topbar-notifications,bottom-nav}.blade.php` (nuovi), `resources/css/filament/mobile-shell.css` (nuovo), `resources/css/filament/{admin,gr,sezione}/theme.css` (aggiunto `@import '../mobile-shell.css';`).
- Verifica: `sail composer qa` verde (Pint OK, Larastan 0 errori, Pest 235 test passati/495 assertion, nessuna regressione). Nessun tool MCP browser/screenshot disponibile in questa sessione (confermato di nuovo, vedi Codebase Patterns): verificato invece via `sail artisan tinker` — richiesta HTTP simulata come utente autenticato reale (seed `LocalDevSeeder`) su `/sezione`, `/gr`, `/admin` → 200 OK, markup `fi-mobile-bottom-nav` e brand name presenti in tutti e 3; su `/sezione/calendario` la voce "Calendario" risulta correttamente evidenziata (`text-[color:var(--text-brand)]`) mentre "Home" no. Confronto pixel con `mobile-3-ruoli.dc.html` resta da fare manualmente (nessun tool screenshot disponibile).
- **Learnings for future iterations:**
  - Vedi le nuove voci in Codebase Patterns sopra (render hook scopes, route naming, verifica senza browser via tinker) — sono generali e riutilizzabili per tutte le story mobile successive (US-005, US-007, US-013, US-015, US-018, US-021, US-026, US-028).
  - Il bell icon in topbar è puramente decorativo/disabled: se una story futura introduce notifiche reali, dovrà sostituire `mobile-topbar-notifications.blade.php` con `@livewire(Filament\Livewire\DatabaseNotifications::class)` e abilitare `->databaseNotifications()` sui 3 panel (richiede la migrazione standard Laravel `notifications`, non presente in questo repo) — esplicitamente fuori scope per US-002.
---

## [2026-07-12] - US-003
- Aggiunto `colore_hex` (string, nullable, 7 char) al modello `Torre` via nuova migrazione `2026_07_12_184455_add_colore_hex_to_torri_table.php`, con backfill delle 2 torri esistenti in `TorriSeeder` (Torre 1 = `#1D574B` pino, Torre 2 = `#C77E2A` larice — stessi hex già usati nel tema Filament di US-001 e coerenti coi mockup), e default `faker->hexColor()` in `TorreFactory` per i test.
- Nuovo helper `Torre::coloreHexPer(?Torre $torre): string` (con costante `Torre::COLORE_DEFAULT = '#64748B'`) come **unica fonte** del colore effettivo di una torre (gestisce sia torre null che colore_hex non ancora impostato) — sostituisce 4 calcoli precedentemente duplicati e incoerenti tra loro (fix BUG-07):
  1. `CalendarioPrenotazioniWidget::torriColori()` (Sezione, condiviso dal blade GR) usava una palette hardcoded indicizzata per posizione (`['#2563eb', '#ea580c', ...]`), non per identità della torre — ora legge `colore_hex` per id.
  2. Le legende hardcoded nei blade `gr/pages/calendario.blade.php` e `sezione/pages/calendario.blade.php` (stessi 2 hex copiati a mano, etichette statiche "Torre 1"/"Torre 2" indipendenti dai nomi reali) — ora entrambe iterano `$this->getTorri()` e mostrano nome + `Torre::coloreHexPer($torre)` reali.
  3. I badge "torre.nome" nelle tabelle Prenotazione di Gr e Sezione usavano `match ($record->torre_id) { 1 => 'info', 2 => 'warning', default => 'gray' }` (colore semantico Filament agganciato all'id, non al colore reale) — ora `Color::hex(Torre::coloreHexPer($record->torre))`.
  4. Il badge "torre.nome" in Admin (Prenotazione) non aveva alcun colore — allineato agli altri 2 pannelli per coerenza.
- Stesso trattamento anche per le tab "Dettagli" delle pagine `ViewPrenotazione` (Gr e Sezione), che avevano il badge torre senza colore.
- `TorreResource` (Admin): nuovo campo `ColorPicker::make('colore_hex')` nel form (con `->regex('/^#[0-9A-Fa-f]{6}$/')` oltre al color-picker nativo, per bloccare input manuale non valido) e nuova colonna badge colorata nella tabella (stesso helper `Torre::coloreHexPer()`).
- Files changed: `database/migrations/2026_07_12_184455_add_colore_hex_to_torri_table.php` (nuovo), `app/Models/Torre.php`, `database/factories/TorreFactory.php`, `database/seeders/TorriSeeder.php`, `app/Filament/Admin/Resources/{TorreResource,PrenotazioneResource}.php`, `app/Filament/Gr/Resources/PrenotazioneResource.php`, `app/Filament/Gr/Resources/PrenotazioneResource/Pages/ViewPrenotazione.php`, `app/Filament/Sezione/Resources/PrenotazioneResource.php`, `app/Filament/Sezione/Resources/PrenotazioneResource/Pages/ViewPrenotazione.php`, `app/Filament/Sezione/Widgets/CalendarioPrenotazioniWidget.php`, `resources/views/filament/{gr,sezione}/pages/calendario.blade.php`, `tests/Feature/ColoreTorreConsistencyTest.php` (nuovo).
- Nuovo test Feature `ColoreTorreConsistencyTest`: crea una torre con `colore_hex` esplicito, una prenotazione `inviata` collegata, verifica che (a) l'evento calendario (`CalendarioPrenotazioniWidget::fetchEvents()`) usi esattamente quel colore come `backgroundColor`, e (b) le colonne badge "torre.nome" delle tabelle Prenotazione **sia** GR **sia** Sezione (ottenute via `Livewire::test(ListPrenotazioni::class)->instance()->getTable()->getColumn('torre.nome')->record($prenotazione)`) restituiscano lo stesso `Color::hex($torre->colore_hex)`.
- Verifica: `sail composer qa` verde (Pint OK, Larastan 0 errori, Pest 236 test passati/498 assertion — +1 test, nessuna regressione). Verifica end-to-end via `sail artisan tinker` (nessun tool MCP browser disponibile, vedi Codebase Patterns): dopo aver girato la nuova migrazione + re-seed di `TorriSeeder` sul DB locale, richiesta autenticata reale su `/sezione/calendario` conferma che la legenda mostra il nome vero della torre col suo `colore_hex` nell'HTML; su una prenotazione reale già seedata con torre assegnata, `CalendarioPrenotazioniWidget::fetchEvents()` e `Torre::coloreHexPer()` restituiscono lo stesso identico hex (`#1D574B`); la form di modifica torre in Admin (`/admin/torres/{id}/edit`) contiene il campo `colore_hex`. Confronto pixel con i mockup resta da fare manualmente.
- **Learnings for future iterations:**
  - Vedi le 2 nuove voci in Codebase Patterns sopra (badge colore dinamico via `Color::hex()` in closure, e il falso positivo Larastan `nullsafe.neverNull` su relazioni `BelongsTo` nullable) — rilevanti per qualunque story futura che leghi un colore/badge a un record correlato opzionale.
  - Le TorreResource read-only di Gr e Sezione (`app/Filament/{Gr,Sezione}/Resources/TorreResource.php`) NON sono state toccate in questa story (nessun AC lo richiedeva): non mostrano ancora uno swatch colore nella loro tabella/infolist. Se una story futura (es. US-029 "Gestione torri desktop") volesse mostrare il colore anche lì, riusare `Torre::coloreHexPer()`.
---

## [2026-07-12] - US-005
- Restyle mobile della dashboard Sezione riusando lo stesso `PrenotazioniDashboardWidget`/vista di US-004 (nessun componente/layout parallelo), coerente col mockup "Mobile Sezione Dashboard" (`mobile-3-ruoli.dc.html`): la shell mobile (topbar, bottom nav Home/Nuova/Prenotazioni/Calendario) era già a posto da US-002, non toccata.
- Nel ramo "prenotazione attiva" della vista: il pulsante header "Apri calendario" ora è `hidden md:inline-flex` (nascosto sotto il breakpoint 768px della shell mobile), "Vedi dettaglio" diventa `w-full` sotto 768px (full-width come nel mockup) e `md:w-auto` sopra. Aggiunta una nuova griglia 2 colonne `md:hidden` con 2 "quick action" a card (Nuova prenotazione, Calendario torri — stessi URL già esposti dal widget: `getUrlNuovaPrenotazione()`/`getUrlCalendario()`), visibile solo sotto 768px, subito sotto la card principale — replica esatta della sezione equivalente nel mockup mobile. Il ramo "stato vuoto" (nessuna prenotazione attiva) non ha un mockup mobile dedicato e non è stato toccato: le sue 2 CTA erano già impilabili (flex column) a quella larghezza.
- Wrapper `<div class="flex flex-col gap-4">` introdotto attorno a card principale + griglia quick-action (prima erano 2 div fratelli senza gap tra loro) — solo nel ramo "attiva", non tocca il ramo "stato vuoto".
- Icone: `heroicon-o-plus` (stessa già usata per "Nuova" nella bottom nav di US-002, per coerenza) e `heroicon-o-calendar-days` (già usata altrove in questo stesso blade). Il mockup usa icone Lucide (`calendar-plus` non esiste nel set Heroicons outline vendorizzato, verificato con `find vendor/blade-ui-kit/blade-heroicons/resources/svg -iname "o-calendar*"` — solo `o-calendar`, `o-calendar-date-range`, `o-calendar-days`).
- Files changed: `resources/views/filament/sezione/widgets/prenotazioni-dashboard.blade.php` (nessun cambio al widget PHP, nessun nuovo metodo necessario: tutti gli URL usati esistevano già da US-004).
- Verifica: `sail composer qa` verde (Pint OK, Larastan 0 errori/157 file, Pest 240 test passati/509 assertion — nessuna regressione, nessun nuovo test Pest aggiunto: l'AC di questa story richiede solo verifica visiva + qa verde, non nuova copertura test, e la vista non ha logica nuova da testare oltre a quella già coperta da `PrenotazioniDashboardWidgetTest`). Verifica end-to-end via `sail artisan tinker` (nessun tool MCP browser disponibile, confermato ancora): richiesta autenticata reale su `/sezione` con una prenotazione reale (status Approvata, pdf_firmato_at null, torre assegnata) creata ad-hoc e poi ripulita → l'HTML contiene la griglia `grid grid-cols-2 gap-3 md:hidden`, entrambe le label "Nuova prenotazione"/"Calendario torri", la classe `hidden md:inline-flex` sul pulsante "Apri calendario", e la bottom nav (`fi-mobile-bottom-nav`) di US-002. Confronto pixel reale a viewport 390x844 contro il mockup resta da fare manualmente (nessun tool screenshot disponibile in questa sessione).
- **Learnings for future iterations:**
  - Per creare una `Prenotazione` di prova su un DB seedato in tinker, usare sempre `sezione_id`/`sottosezione_id` dell'utente reale scelto (vedi nota già in Codebase Patterns su US-004) e ricordarsi di ripulire il record creato a fine verifica (`Prenotazione::find($id)?->delete()`), per non lasciare dati fittizi nel DB di sviluppo condiviso.
  - Pattern "azione full-width su mobile / auto-width su desktop + azione secondaria nascosta su mobile e spostata altrove" (qui: "Apri calendario" nascosto in header, riproposto come quick-action card) è probabilmente riutilizzabile per le prossime story mobile del wizard (US-007) e delle liste (US-013, US-015) quando il mockup mobile riorganizza le azioni rispetto al desktop invece di limitarsi a impilarle.
---

## [2026-07-12] - US-004
- Restyle di `PrenotazioniDashboardWidget` (Sezione), estendendo il widget e la sua vista esistenti (nessun componente parallelo), secondo i due mockup `pannello-sezione.dc.html` "Dashboard prenotazione attiva" / "Dashboard stato vuoto".
- Widget PHP: 3 nuovi metodi pubblici — `getUrlDettaglio(Prenotazione)` (→ `PrenotazioneResource::getUrl('view', ['record' => ...])`), `getUrlCalendario()` (→ `CalendarioPage::getUrl()`), `getScadenzaPdfFirmato(Prenotazione): ?Carbon` (banner "prossimo passo: carica il PDF firmato" — mostrato solo se `status === Approvata` e `pdf_firmato_at === null`, scadenza = `data_inizio_evento->copy()->subDays(GrSettings::giorni_minimi_caricamento_documenti)`, stessa formula di `ReminderT10Notification`, ma con `->copy()` esplicito per non mutare l'attributo Carbon in place come fa invece — con un piccolo bug preesistente non toccato in questa story — la notification originale).
- Blade: card "prenotazione attiva" con titolo evento, badge stato (`$attiva->status->color()`, non più un `match()` duplicato), pillola torre con pallino colorato via `Torre::coloreHexPer($attiva->torre)` (stesso pattern di US-003) + testo pill neutro (non ho introdotto tinte-per-torre dedicate: solo il pallino porta il colore identificativo, la pillola resta stone/neutral), griglia 3 colonne (periodo evento / ritiro-riconsegna / **deposito torre sempre visibile, BUG-08**), CTA "Vedi dettaglio" + "Apri calendario", banner condizionale larch per il promemoria PDF. Stato vuoto: icona cerchio con token `--surface-subtle`/`--text-brand`, messaggio di invito, 2 CTA (Nuova prenotazione / Vedi disponibilità torri → calendario).
- Bugfix minore nel passaggio: nel markup precedente `$attiva->torre->nome` e `$attiva->torre->indirizzo_deposito` usavano `->` non-nullsafe nonostante `torre_id` sia nullable — corretto a `?->` (il fallback "non ancora assegnata" ora non crasha più se una prenotazione attiva non ha ancora una torre).
- Nuovo test Feature `tests/Feature/PrenotazioniDashboardWidgetTest.php` (4 test): stato vuoto, card con torre/deposito/badge, banner PDF mostrato quando pertinente, banner PDF assente quando il PDF è già caricato.
- Files changed: `app/Filament/Sezione/Widgets/PrenotazioniDashboardWidget.php`, `resources/views/filament/sezione/widgets/prenotazioni-dashboard.blade.php`, `tests/Feature/PrenotazioniDashboardWidgetTest.php` (nuovo).
- Verifica: `sail composer qa` verde (Pint OK, Larastan 0 errori, Pest 240 test passati/509 assertion — +4 test, nessuna regressione). Verifica end-to-end via `sail artisan tinker` (nessun tool MCP browser disponibile, confermato ancora una volta): richiesta autenticata reale su `/sezione` con nessuna prenotazione attiva → mostra "Nessuna prenotazione attiva"; con una prenotazione reale creata ad-hoc (status Approvata, pdf_firmato_at null, torre reale con colore e indirizzo) → HTML contiene il nome evento, lo `style="background-color:{hex reale della torre}"`, l'indirizzo di deposito reale, il banner "Prossimo passo" e la CTA "Vedi dettaglio". Confronto pixel con il mockup resta da fare manualmente.
- **Learnings for future iterations:**
  - Per creare una `Prenotazione` via factory in tinker/debug su un DB già seedato (LocalDevSeeder, 229 sezioni con codici 1-229), **non** lasciare che la factory generi una nuova `Sezione`/`Sottosezione` random: il `codice` fake di Faker può collidere con un codice già esistente (`UniqueConstraintViolationException` su `sezioni_codice_unique`). Passare sempre `sezione_id => $user->sezione_id, sottosezione_id => $user->sottosezione_id` espliciti quando si crea una prenotazione per uno user seedato esistente.
  - Elenco icone Heroicons effettivamente disponibili (pacchetto `blade-ui-kit/blade-heroicons` vendorizzato): verificare sempre con `find vendor/blade-ui-kit/blade-heroicons/resources/svg -iname "o-<nome>*"` prima di usare un nome heroicon in un Blade — non tutti i nomi "plausibili" esistono (es. `heroicon-o-document-exclamation` NON esiste, usare `heroicon-o-exclamation-triangle`).
  - `x-filament::button` (componente anonimo in `vendor/filament/support/resources/views/components/button/index.blade.php`) supporta i prop `color`, `outlined` (bool), `size` (xs/sm/md/lg/xl), `tag="a"` + `href`, `icon` — utile per restyle futuri senza dover cercare la sorgente ogni volta.
---
