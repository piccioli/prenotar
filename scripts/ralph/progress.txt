# Ralph Progress Log
Started: Mer  8 Lug 2026 16:40:51 CEST
---

## Codebase Patterns
- Per bloccare l'accesso a una pagina Filament CreateRecord/EditRecord prima che il form venga renderizzato (early guard), overridare `protected static string $view` con una blade custom in `resources/views/filament/<panel>/resources/<resource>/pages/<page>.blade.php`, poi overridare `mount()`: chiamare `$this->authorizeAccess()`, valorizzare una proprietà pubblica (es. `?Model $xxx`), e chiamare `$this->fillForm()`/`$this->previousUrl = url()->previous()` SOLO se la condizione di blocco non scatta. La blade custom copia il markup originale del vendor (`vendor/filament/filament/resources/views/resources/pages/create-record.blade.php`) e aggiunge un `@if` per mostrare un banner `<x-filament::section>` + `<x-filament::link>` al posto del form.
- Nessun dev-browser/browser-tool MCP disponibile in questo ambiente Ralph: la "Verify in browser" delle acceptance criteria va soddisfatta con Feature test HTTP (`->get(...)->assertSuccessful()->assertSee(...)`), che renderizzano il markup Blade/Livewire reale end-to-end. Documentare esplicitamente nel progress log quando questo sostituto viene usato.
- Il modello `Prenotazione` ha già uno scope `scopeAttive()` (esclude solo Concluso/Annullata) usato per liste/archivio; è diverso dalla regola di validazione `UnicaPrenotazioneAttivaPerUser` (usata al submit finale) che esclude anche Bozza e InviatoAssicurazione oltre a Concluso/Annullata. Non confondere le due definizioni di "attiva" quando si leggono le acceptance criteria.

## [2026-07-08] - US-001
- Implementato il blocco anticipato del wizard di creazione prenotazione: se l'utente loggato (sezione o sottosezione) ha già una prenotazione con stato diverso da Annullata/Concluso, la pagina di creazione mostra un banner Filament con nome evento, date e link al dettaglio, senza renderizzare alcun campo del form.
- File modificati:
  - app/Filament/Sezione/Resources/PrenotazioneResource/Pages/CreatePrenotazione.php (override `mount()` + `$view`, nuovo metodo privato `trovaPrenotazioneAttivaEsistente()` basato su `Prenotazione::query()->attive()` filtrato per sezione_id/sottosezione_id dell'utente)
  - resources/views/filament/sezione/resources/prenotazione-resource/pages/create-prenotazione.blade.php (nuova vista, copia del vendor con banner condizionale)
  - tests/Feature/Sezione/PrenotazioneBloccoWizardAttivaTest.php (nuovo, 3 test: blocco per sezione, blocco per sottosezione, nessun blocco se solo Annullata)
- Verifiche eseguite: `sail composer test` (228 passed), `sail composer phpstan` (0 errori), `sail composer pint` (passed).
- **Learnings for future iterations:**
  - Vedi sezione "Codebase Patterns" sopra per il pattern di override `$view`/`mount()` su pagine Filament CreateRecord, riusabile per altre story del wizard (US-005, US-007 toccano lo stesso file/step).
  - Il controllo esistente lato submit (`UnicaPrenotazioneAttivaPerUser`, in `app/Filament/Sezione/Resources/PrenotazioneResource.php` e `PrenotazioneStateMachine`) NON è stato toccato, come richiesto dalle acceptance criteria — resta una rete di sicurezza aggiuntiva keyed su `user_id`, distinta dal nuovo controllo keyed su `sezione_id`/`sottosezione_id`.
  - Nessun browser tool disponibile in questo ambiente: la verifica UI è stata fatta tramite Feature test HTTP reali (non solo unit/mock).
---

## [2026-07-08] - US-003
- Rimosso completamente il campo `responsabile_codice_cai` (segnalato come non verificabile) da database, modello, form Filament (Sezione create/edit), infolist di dettaglio (Sezione e Gr) e template PDF "Richiesta parete". Il campo `responsabile_titolo_cai` (diverso, non toccato) resta invariato.
- File modificati:
  - database/migrations/2026_07_08_170000_drop_responsabile_codice_cai_from_prenotazioni.php (nuova, dropColumn / down ripristina la colonna con `->after('responsabile_titolo_cai')`)
  - app/Models/Prenotazione.php (rimosso da `$fillable`; non era presente nel docblock `@property`, quindi nessuna modifica lì)
  - database/factories/PrenotazioneFactory.php (rimossa riga `'responsabile_codice_cai' => null`)
  - app/Filament/Sezione/Resources/PrenotazioneResource.php (rimosso il TextInput in entrambe le definizioni di form — quella a step del wizard/create e quella a Section per l'edit — la Grid::make(2) che accoppiava titolo_cai+codice_cai è stata sciolta lasciando titolo_cai come campo singolo)
  - app/Filament/Sezione/Resources/PrenotazioneResource/Pages/ViewPrenotazione.php e app/Filament/Gr/Resources/PrenotazioneResource/Pages/ViewPrenotazione.php (rimosso il TextEntry corrispondente nell'infolist "Responsabile in loco")
  - resources/views/pdf/richiesta_parete.blade.php (rimossa la riga tabella "Codice CAI")
  - Admin non referenziava il campo, nessuna modifica necessaria lì. Nessun test esistente referenziava il campo (verificato con grep su tests/), quindi nessun test da aggiornare.
- Verifiche eseguite: `grep -r responsabile_codice_cai` su app/, resources/, database/factories/, tests/ (residuo solo nella nuova migrazione stessa e in documentazione/prd, come atteso), `sail artisan migrate` (nuova migrazione applicata senza errori), `sail composer qa` = pint passed + phpstan 0 errori + 228 test passed.
- **Learnings for future iterations:**
  - `PrenotazioneResource.php` definisce il form del "Responsabile in loco" in DUE punti distinti (uno per lo step del wizard di create, uno per la Section usata in edit/altre view) — quando si tocca un campo di quello step bisogna cercare e modificare entrambe le occorrenze, non solo la prima trovata.
  - US-006 ("UI: rimozione campo codice CAI dal form responsabile") risulta già completamente soddisfatta da questa story: tutte le sue acceptance criteria (rimozione dal form Sezione, da infolist Gr/Admin, dal PDF, nessun test residuo) sono già verificate qui. La prossima iterazione su US-006 dovrebbe limitarsi a ri-verificare (grep + test) e marcarla passata, senza codice aggiuntivo previsto.
---

## [2026-07-08] - US-002
- Implementati i due nuovi enum e le relative colonne per distinguere trasporto con mezzo aziendale vs privato (con categoria patente).
- File modificati:
  - app/Enums/TipoMezzo.php (nuovo, backed string enum: Aziendale='aziendale', Privato='privato', con label())
  - app/Enums/CategoriaPatente.php (nuovo, backed string enum: B='B', BE='BE', label() restituisce "B+E" per BE)
  - database/migrations/2026_07_08_164918_add_tipo_mezzo_to_prenotazioni.php (nuova, aggiunge tipo_mezzo string default 'aziendale' e categoria_patente_privato string nullable, dopo azienda_trasporto)
  - app/Models/Prenotazione.php (aggiunti tipo_mezzo/categoria_patente_privato a $fillable, casts() e docblock @property)
- Verifiche eseguite: `sail artisan migrate` (nuova migrazione applicata senza errori su schema esistente), `sail composer test` (228 passed, invariati — story è solo schema, nessun nuovo comportamento utente da testare in questa iterazione, la UI arriva in US-005), `sail composer phpstan` (0 errori), `sail composer pint` (passed).
- **Learnings for future iterations:**
  - Pattern enum di dominio confermato: enum PHP backed string in app/Enums/, con metodo label() a match esplicito per ogni case (vedi ResponsabileTipo.php come riferimento). Le option Filament si costruiscono con `collect(Enum::cases())->mapWithKeys(fn ($c) => [$c->value => $c->label()])` (vedi PrenotazioneResource.php righe ~203, ~379).
  - Le migrazioni "aggiungi colonna" successive alla create_prenotazioni_table originale seguono il pattern: `Schema::table('prenotazioni', fn (Blueprint $table) => ...)` con `->after('colonna_precedente')` per mantenere l'ordine leggibile, e down() con dropColumn simmetrico (vedi add_reminder_flags_to_prenotazioni.php).
  - US-005 (UI toggle mezzo privato) consumerà direttamente questi due enum e le due colonne appena introdotte nello stesso file toccato da US-001 (CreatePrenotazione.php) — nessun lavoro di schema aggiuntivo necessario per quella story.
---

## [2026-07-08] - US-004
- Aggiunte le colonne di audit per la conferma di lettura del manuale d'istruzioni per-torre: `manuale_letto_confermato_at` (timestamp nullable) e `manuale_letto_torre_id` (FK nullable verso `torri.id`, nullOnDelete).
- File modificati:
  - database/migrations/2026_07_08_180000_add_manuale_letto_to_prenotazioni.php (nuova, `foreignId('manuale_letto_torre_id')->nullable()->constrained('torri')->nullOnDelete()`, down() con `dropConstrainedForeignId` + `dropColumn`)
  - app/Models/Prenotazione.php (aggiunti i due campi a `$fillable`, cast `datetime` per `manuale_letto_confermato_at`, docblock @property aggiornato)
- Verifiche eseguite: `sail artisan migrate` (nuova migrazione applicata senza errori sullo schema esistente), `sail composer qa` = pint passed + phpstan 0 errori + 228 test passed (nessun nuovo test necessario, story è solo schema come le acceptance criteria richiedono — nessun nuovo comportamento utente da testare, la UI arriva in US-007).
- **Learnings for future iterations:**
  - Per una colonna FK nullable aggiunta in una migrazione successiva (non quella di creazione tabella), usare `$table->foreignId('col')->nullable()->after('precedente')->constrained('tabella')->nullOnDelete()`; nel down() usare `dropConstrainedForeignId('col')` (droppa sia la FK che la colonna) invece di `dropColumn` + drop manuale della foreign key.
  - US-007 (UI flag lettura manuale) consumerà direttamente queste due colonne nello stesso file toccato da US-001/US-005 (CreatePrenotazione.php) — nessun lavoro di schema aggiuntivo necessario per quella story.
---

## [2026-07-08] - US-005
- Implementata la UI del wizard per dichiarare il tipo di mezzo (aziendale/privato) con categoria patente obbligatoria in caso di mezzo privato, usando gli enum e le colonne introdotti in US-002.
- File modificati:
  - app/Filament/Sezione/Resources/PrenotazioneResource.php (radio `tipo_mezzo` + select `categoria_patente_privato` condizionale/`required` via `Forms\Get`, aggiunti sia allo step wizard di create sia alla Section usata in edit; placeholder di riepilogo "Trasporto" nello step finale del wizard)
  - app/Filament/Sezione/Resources/PrenotazioneResource/Pages/ViewPrenotazione.php e app/Filament/Gr/Resources/PrenotazioneResource/Pages/ViewPrenotazione.php (TextEntry `tipo_mezzo` e `categoria_patente_privato`, quest'ultimo visibile solo se il mezzo è privato)
  - tests/Feature/Sezione/PrenotazioneTipoMezzoValidationTest.php (nuovo, 3 test: privato senza patente → errore di validazione; privato con patente → salva; aziendale default → nessuna patente richiesta)
- Verifiche eseguite: `sail composer qa` = pint passed + phpstan 0 errori + 231 test passati (`sail composer test` isolato confermato in precedenza).
- **Learnings for future iterations:**
  - Confermato il pattern "doppia occorrenza" già annotato in US-003: `PrenotazioneResource.php` definisce il form Logistica/Trasporto sia nello step del wizard (`wizardSteps()`) sia nella Section di `form()` per l'edit — ogni nuovo campo va aggiunto in entrambi i punti.
  - Per campi condizionali Filament basati su un altro campo dello stesso step, usare `Forms\Get $get` sia in `visible()` che in `required()` (non serve `live()` sul campo dipendente, solo sul campo che lo controlla, qui `tipo_mezzo`).
  - US-007 può riusare lo stesso step "torre" del wizard senza toccare questo file per la parte trasporto: nessuna interferenza prevista con questa story.
---

## [2026-07-08] - US-006
- Story di sola verifica, nessun codice modificato: tutte le acceptance criteria erano già soddisfatte dall'implementazione di US-003 (rimozione completa di responsabile_codice_cai da DB/modello/form/infolist/PDF), come già annotato nei "Learnings" di quella iterazione.
- Verifiche eseguite in questa iterazione:
  - `grep -rn "responsabile_codice_cai" app/ resources/ database/factories/ tests/` → nessun risultato.
  - Verifica mirata su Admin PrenotazioneResource (form + ViewPrenotazione infolist) → nessun riferimento al campo.
  - Verifica su entrambi i template PDF (`resources/views/pdf/richiesta_parete.blade.php` e `modulo3.blade.php`) → nessun riferimento al campo.
  - La colonna resta definita nella migrazione originale `create_prenotazioni_table` (non modificata, come da vincolo) ma viene droppata dalla migrazione successiva introdotta in US-003 — comportamento atteso.
  - `sail composer qa` → pint passed, phpstan 0 errori, 231 test passati (nessuna regressione).
- Nessun file modificato in questa iterazione (solo prd.json e progress.txt).
- **Learnings for future iterations:**
  - Quando una story precedente segnala nei suoi "Learnings" che una story successiva è già soddisfatta, non fidarsi ciecamente: rieseguire comunque i grep/test mirati alle sue acceptance criteria specifiche (qui: anche Admin resource e template Modulo 3, non solo i punti esplicitamente citati in US-003) prima di marcarla passata.
---
